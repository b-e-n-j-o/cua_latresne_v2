[pytest]
testpaths = tests
pythonpath = .
markers =
    smoke: tests bout-en-bout (réseau + Supabase, nécessite AUTH_TEST_EMAIL/PASSWORD)
    llm: ping réel de l'API Gemini (quota / auth / modèle)
filterwarnings =
    ignore::DeprecationWarning
